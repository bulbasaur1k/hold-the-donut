/target
/fuzz/target
/fuzz/corpus
/fuzz/artifacts
/references
**/*.rs.bk
Cargo.lock.lock
.DS_Store
.idea/
.vscode/
*.swp
*.swo
.env
.env.local
/dist
/release
# Cross-built server binary extracted from the Docker image for deploy
/donut-server
# CI artifacts
coverage/
*.profraw
# Local-only testbench data
scripts/xray-testbench/*.key
scripts/xray-testbench/*.crt
scripts/xray-testbench/data/
# Throwaway TLS cert+key generated by xhttp-test.sh (regenerated on demand)
scripts/xray-testbench/certs/
# Upstream xray-core source checkout (ground-truth reference; not vendored)
scripts/xray-testbench/upstream/

# Local session context (operational notes + secrets — never commit)
.session/

# Deploy config with real prod UUIDs (never commit)
scripts/xray-testbench/server-xhttp-443.toml
